{
  "name": "parkplatz-android",
  "version": "1.0.3",
  "private": true,
  "description": "Android (Capacitor) wrapper for the Parkplatz puzzle game",
  "scripts": {
    "build:web": "node scripts/build-www.mjs",
    "prepare:android": "node scripts/build-www.mjs && (test -d android || npx --yes cap add android) && npx cap sync android && node scripts/patch-android.mjs",
    "assets": "capacitor-assets generate --iconBackgroundColor '#141a26' --splashBackgroundColor '#141a26' --splashBackgroundColorDark '#0b0e15'",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  }
}
